

spring:

  server:
    port: ${PORT:8080}

  main:
      web-application-type: servlet

  mail:
    host: ${MAIL_HOST}
    port: ${MAIL_PORT}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: ${SERVER_PORT}

security:
  jwt:
    secret: ${JWT_SECRET}
    issuer: ${JWT_ISSUER:getintouch-mk7b.onrender.com}
    access-ttl-seconds: 900
    refresh-ttl-seconds: 604800
    refresh-token-cookie-name: refreshToken

    cookie-secure: true        # 🔥 REQUIRED (HTTPS)
    cookie-http-only: true
    cookie-same-site: none     # 🔥 REQUIRED for cross-origin
    # ❌ DO NOT SET cookie-domain (important)


app:
  bootstrap:
    admin:
      email: ${ADMIN_EMAIL}
      password: ${ADMIN_PASSWORD}

cloudflare:
  r2:
    access-key: ${R2_ACCESS_KEY}
    secret-key: ${R2_SECRET_KEY}
    endpoint: ${R2_ENDPOINT}
    bucket: ${R2_BUCKET}
    public-url: ${R2_PUBLIC_URL}